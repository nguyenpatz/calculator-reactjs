# Build calculator with reactjs (useReducer)

my app have state object includes
state = {
	currentOperand,
	previousOperand,
	opreration
}

and function reducer

# Features
add digit, choose operation, delete digit, clear, evaluate

